<!DOCTYPE html>
<html>
<head>
  <title>Leave Success</title>
</head>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  
<style>
   body {
      margin: 0; /* Remove any gaps above/below */
      background-color: #f8f9fa; /* Light background */
      font-family: 'Arial', sans-serif;
    }
    .navbar {
      background-color: #ffffff; /* White navbar */
      padding: 0.5rem 1rem;
      box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
    }
    .navbar-brand {
      font-weight: bold;
      color: #000000; /* Black text */
      font-size: 1.25rem;
    }
    .navbar-nav .nav-link {
      font-weight: 500;
      color: #000000; /* Black text */
      margin-right: 1rem;
      font-size: 1rem;
    }
    .navbar-nav .nav-link:hover {
      color: #0d6efd; /* Primary blue on hover */
    }
    .btn-logout {
      font-weight: 500;
      color: #ffffff;
      background-color: #0d6efd; /* Blue button */
      border: none;
      padding: 0.4rem 1rem;
      font-size: 0.9rem;
      border-radius: 0.25rem;
    }
    .btn-logout:hover {
      background-color: #084298; /* Darker blue on hover */
    }
    .container {
      margin-top: 2rem;
    }
    h2 {
      font-weight: bold;
      color: #000000; /* Black text for titles */
    }
    table {
    width: 200px; /* Full width */
    border-collapse: collapse; /* Remove default spacing between cells */
    margin-top: 1.5rem;
    background-color: #ffffff; /* White background for the table */
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1); /* Subtle shadow */
    border-radius: 0.5rem; /* Rounded corners */
    overflow: hidden; /* Prevent content overflow */
  }

  thead {
    background-color: #0d6efd; /* Blue background for header */
    color: #ffffff; /* White text for header */
  }

  th, td {
    padding: 0.75rem; /* Add padding for readability */
    text-align: center; /* Center-align text */
    border: 1px solid #dee2e6; /* Light gray border for cells */
  }

  tbody tr:nth-child(odd) {
    background-color: #f8f9fa; /* Light gray background for alternate rows */
  }

  tbody tr:hover {
    background-color: #e9ecef; /* Slightly darker background on hover */
  }
</style>
<body>
  <%- include('user-header') %>
<br><br><br><br>
  <h1><%= message %></h1>
  <table class="table table-bordered table-hover">
    <thead>
      <tr>
        <th>Leave Types</th>
        <th>CL</th>
        <th>SL</th>
        <th>EL</th>
        <th>SCL</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <th>Leaves at Credit</th>
        <td><%= leaveData.before.cl %></td>
        <td><%= leaveData.before.sl %></td>
        <td><%= leaveData.before.el %></td>
        <td><%= leaveData.before.scl %></td>
      </tr>
      <tr>
        <th>Balance</th>
        <td><%= leaveData.after.cl %></td>
        <td><%= leaveData.after.sl %></td>
        <td><%= leaveData.after.el %></td>
        <td><%= leaveData.after.scl %></td>
      </tr>
    </tbody>
  </table>
  
  
</body>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</html>
