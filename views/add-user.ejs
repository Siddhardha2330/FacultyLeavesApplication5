
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Add User</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>

    body {
      margin: 0;
      background-color: #f8f9fa; /* Light gray background */
      font-family: 'Arial', sans-serif;
    }
    .btn-add-user {
      background-color: #0d6efd; /* Blue button */
      color: #ffffff;
      border: none;
      padding: 0.8rem 1.5rem; /* Similar padding as the submit button */
      font-size: 1rem;
      border-radius: 0.5rem;
      box-shadow: 0px 8px 15px rgba(0, 0, 0, 0.2); /* Elevated effect */
      position: fixed;
      bottom: 3.5rem;
      right: 10rem; /* Adjusted position to be consistent */
      z-index: 1000; /* Ensure it stays above other elements */
    }

    .btn-add-user:hover {
      background-color: #084298; /* Darker blue on hover */
      box-shadow: 0px 8px 20px rgba(0, 0, 0, 0.3); /* Enhanced shadow on hover */
    }
    .navbar {
      background-color: #ffffff; /* White navbar */
      padding: 0.5rem 1rem;
      box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1); /* Subtle shadow */
    }
    .navbar-brand {
      font-weight: bold;
      color: #000000; /* Black text */
      font-size: 1.25rem;
    }
    .navbar-nav .nav-link {
      font-weight: 500;
      color: #000000; /* Black text */
      margin-right: 1rem;
      font-size: 1rem;
    }
    .navbar-nav .nav-link:hover {
      color: #0d6efd; /* Primary blue on hover */
    }
    .btn-logout {
      font-weight: 500;
      color: #ffffff;
      background-color: #0d6efd; /* Blue button */
      border: none;
      padding: 0.4rem 1rem;
      font-size: 0.9rem;
      border-radius: 0.25rem;
    }
    .btn-logout:hover {
      background-color: #084298; /* Darker blue on hover */
    }
    .container {
      margin-top: 5rem; /* Space below navbar */
    }
    h2 {
      font-weight: bold;
      color: #000000; /* Black text for headings */
      margin-bottom: 1rem;
    }
    .form-container {
      background: #ffffff; /* White card-style form */
      padding: 2rem;
      border-radius: 0.5rem;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
      width: 60%;
    }
    .form-label {
      font-weight: 500;
      color: #333333; /* Darker text */
    }
    .btn-submit {
      background-color: #0d6efd; /* Blue button */
      color: #ffffff;
      border: none;
      padding: 0.5rem 1rem;
      font-size: 1rem;
      border-radius: 0.25rem;
    }
    .btn-submit:hover {
      background-color: #084298; /* Darker blue on hover */
    }
    .btn-back {
      color: #ffffff;
      background-color: #6c757d; /* Secondary gray button */
      border: none;
      padding: 0.5rem 1rem;
      font-size: 1rem;
      border-radius: 0.25rem;
      margin-right: 1rem;
    }
    .btn-back:hover {
      background-color: #5a6268; /* Darker gray on hover */
    }
    .btn-submit {
  background-color: #0d6efd;
  color: #ffffff;
  border: none;
  padding: 0.5rem 1rem;
  font-size: 1rem;
  border-radius: 0.25rem;
  width: 100%; /* Make the button span the full width */
  margin-top: 1rem; /* Add space at the top */
}
  </style>
</head>
<body>
  <!-- Navbar -->
   <%- include('admin-header') %>
  <!-- Content Section -->
  <div class="container">
    <h2>Add New User</h2>
    <div class="form-container">
      <!-- Back Button -->
  

      <!-- Message Display -->
      <div id="resultMessage">
        <% if (message) { %>
          <div class="alert alert-<%= message.type %>" role="alert">
            <%= message.text %>
          </div>
        <% } %>
      </div>

      <!-- Form -->
      <form action="/facultyleavesinfo/admin/add-user" method="POST">
        <div class="mb-3">
          <label for="fid">Faculty ID</label>
          <input type="text" name="fid" id="fid" class="form-control" required>
        </div>
        <div class="mb-3">
          <label for="name">Name</label>
          <input type="text" name="name" id="name" class="form-control" required>
        </div>
        
         
          <input type="hidden" name="password" id="password" class="form-control" value="svecwit" required>
      
       
          <input type="hidden" name="status" id="status" class="form-select" value="user" required>
     
        <div class="mb-3">
          <label for="dateofjoining">Date of Joining</label>
          <input type="date" name="dateofjoining" id="dateofjoining" class="form-control" required>
        </div>
        <div class="mb-3">
          <label for="email">Email</label>
          <input type="email" name="email" id="email" class="form-control" required>
        </div>
        <div class="mb-3">
          <label for="designation" class="form-label">Designation</label>
          <select name="designation" id="designation" class="form-select" required>
            <option value="">Select Designation</option>
            <option value="Assistant Professor">Assistant Professor</option>
            <option value="Associate Professor">Associate Professor</option>
            <option value="Professor">Professor</option>
            <option value="Lab Programmer">Lab Programmer</option>
            <option value="Operator">Operator</option>
          </select>
        </div>
        
        <div class="mb-3">

          <label for="mobile">Mobile</label>
          <input type="text" name="mobile" id="mobile" class="form-control" required>
        </div>
        <div class="mb-3">
          <label for="cl">CL</label>
          <input type="number" name="cl" id="cl" class="form-control" required>
        </div>
        <div class="mb-3">
          <label for="sl">SL</label>
          <input type="number" name="sl" id="sl" class="form-control" required>
        </div>
        <div class="mb-3">
          <label for="el">EL</label>
          <input type="number" name="el" id="el" class="form-control" required>
        </div>
        <div class="mb-3">
          <label for="scl">SCL</label>
          <input type="number" name="scl" id="scl" class="form-control" required>
        </div>
       
        <button type="submit" class="btn btn-add-user">Add User</button>

      </form>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

